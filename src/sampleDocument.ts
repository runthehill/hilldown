export const sampleDocument = `# HillDown

A quiet place to write and read Markdown. Type on the left; watch it set itself on the right.

## Getting started

Begin a new line with **/** to open the command menu — try \`/table\`, \`/task\`, or \`/code\`. Select a word and press **⌘B** for bold, **⌘I** for italic, or **⌘K** to wrap a [link](https://example.com).

> Small tools stay useful when the main path stays quiet.

## Field notes

| Stage | Owner | Next step |
| --- | --- | --- |
| Draft | Jules | Tighten the outline |
| Review | Mira | Mark the open questions |
| Publish | Sam | Export the final note |

## Today

- [x] Capture the rough notes
- [x] Separate decisions from follow-ups
- [ ] Send the final summary

Inline \`code\` stays in the flow, while fenced blocks stand on their own:

\`\`\`ts
const note = "write something worth reading";
console.log(note);
\`\`\`
`;
